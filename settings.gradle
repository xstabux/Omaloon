if(JavaVersion.current().ordinal() < JavaVersion.VERSION_16.ordinal()){
    throw new Exception("!YOU MUST USE JAVA 16 OR ABOVE TO COMPILE THIS MOD ! Your java version: ${System.properties["java.version"]}")
}

include "core"
rootProject.name = 'Omaloon'