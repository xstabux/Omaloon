package omaloon.world.meta;

public enum FluidGroup {
	/**
	 * This group specifically acts the same as if the group is null. Only used if blocks that normally override the group have to be independent.
	 */
	unset,

	drills,
	pumps,
	transportation
}
